# TZ_API
🧾 Описание

Этот скрипт — простая демонстрация работы с тестовым API по адресу:

https://test.icorp.uz/interview.php

Он показывает:

как отправить POST-запрос,

как получить ответ от сервера,

как принять входящий запрос от сервера (webhook),

как объединить две части кода,

как отправить их обратно и получить финальное сообщение.

Скрипт выполнен в одном файле.

⚙️ Как работает
1️⃣ Отправляем POST

Скрипт выполняет POST-запрос на API и отправляет JSON с:

msg – тестовым сообщением,

url – адресом текущего файла, куда сервер пришлёт вторую часть кода.

Сервер возвращает первую часть кода.

2️⃣ Ждём вторую часть

API отправляет вторую часть кода на указанный адрес (url) через POST.

Скрипт проверяет каждые 1 секунду — была ли получена запись во фаил part2.txt.

Если в течение 10 секунд вторая часть не пришла — работа прекращается.

3️⃣ Объединяем коды

Первая и вторая части конкатенируются в строку:

fullCode = part1 + part2
4️⃣ Отправляем GET с объединённым кодом

Объединённый код передаётся в виде параметра:

https://test.icorp.uz/interview.php?code=XXXXX

Сервер возвращает итоговое сообщение.

5️⃣ Вывод результата

На экране отображается:

первая часть кода

вторая часть

полный код

итоговое сообщение

🚀 Как запустить

Убедитесь, что PHP установлен.

Запустите локальный сервер, например:

php -S localhost:8000

Откройте в браузере:

http://localhost:8000/index.php

Следуйте выводимым сообщениям.

⚠️ Важно

Сервер test.icorp.uz не сможет обратиться к localhost, если вы запускаете скрипт у себя на компьютере.

Для корректной работы:

используйте публичный хостинг
или

поднимите туннель через ngrok:

ngrok http 8000

И замените url в запросе на адрес, который будет выдан ngrok.

📌 Файл полностью самостоятельный

Вся логика:

приём запросов,

отправка данных,

вывод результатов

выполнена в одном PHP-файле, как указано в задании.

Готово!